/**
 * Commands that do not belong to another specific package
 *
 * Commands should be, by default, placed in packages specific to
 * their function (ex /party commands in party package), however some
 * commands (ex /help, /setspawn, etc) don't map cleanly to a package
 */
package net.lugami.practice.commands;